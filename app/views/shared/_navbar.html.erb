<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand ">ClassRepo</a>
  <div style="display: flex; justify-content: flex-end;"
    <% if user_signed_in? %>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item active">
            <a><%= link_to "Course Index", courses_path, class:"nav-link" %></a>
          </li>
          <% if current_user.picture.attached? %>
            <li><a><%= cl_image_tag current_user.photo.key, class: "avatar dropdown-toggle", id: "navbarDropdown", data: { toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %></a></li>
          <% else %>

          <% end %>
        </ul>
      </div>
    <% else %>
      <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav">
            <li class="nav-item active">
              <a><span class="sr-only"><%= link_to "login", new_user_session_path, class:"nav-link" %></span></a>
            </li>
            <li class="nav-item">
              <a><%= link_to "sign-up", new_user_registration_path, class:"nav-link" %></a>
            </li>
          </ul>
        </div>
    <% end %>
  </div>
</nav>
